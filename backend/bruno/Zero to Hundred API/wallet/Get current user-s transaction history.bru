meta {
  name: Get current user-s transaction history
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/wallet/transactions
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~limit: 
}

auth:bearer {
  token: {{token}}
}
