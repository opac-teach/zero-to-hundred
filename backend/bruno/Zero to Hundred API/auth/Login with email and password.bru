meta {
  name: Login with email and password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{user}}",
    "password": "{{password}}"
  }
}
